{
  "name": "pino",
  "version": "0.0.1",
  "description": "Pino il serverino",
  "main": "dist/server.js",
  "scripts": {
    "prestart": "npm run build",
    "start": "node .",
    "dev": "nodemon src/server.ts",
    "prebuild": "rm -rf dist/*",
    "build": "tslint -c tslint.json -p tsconfig.json --fix && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/policumbent/pino.git"
  },
  "keywords": [],
  "author": "",
  "license": "AGPL",
  "bugs": {
    "url": "https://github.com/policumbent/pino/issues"
  },
  "homepage": "https://github.com/policumbent/pino#readme",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^15.3.1",
    "nodemon": "^2.0.7",
    "prettier": "^2.3.0",
    "ts-node": "^9.1.1",
    "tslint": "^6.1.3",
    "typescript": "^4.2.4"
  }
}
